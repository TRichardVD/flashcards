@eval(await auth.check())
<header>
  <a href="/">
    <img class="logo" src="/resources/assets/logo.svg" alt="Logo" />
  </a>
  <nav>
    @if(auth.isAuthenticated)
      <a href="/user/{{ auth.user.id }}">{{ auth.user.fullName }}</a>
      <a href="/logout">Déconnexion</a>
    @else
      <a href="/login">Connexion</a>
      <a href="/register">Inscription</a>
    @endif
  </nav>
</header>
